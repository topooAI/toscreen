import { execFileSync } from 'node:child_process';import fs from 'node:fs';import path from 'node:path';
const cwd=path.resolve(import.meta.dirname,'..');const run=(args)=>execFileSync('wrangler',args,{cwd,encoding:'utf8'});let databases=JSON.parse(run(['d1','list','--json']));let database=databases.find(item=>item.name==='toscreen-share');if(!database){run(['d1','create','toscreen-share']);databases=JSON.parse(run(['d1','list','--json']));database=databases.find(item=>item.name==='toscreen-share');}if(!database?.uuid)throw new Error('Unable to resolve D1 database');const base=fs.readFileSync(path.join(cwd,'wrangler.toml'),'utf8');const generated=`${base}\n[[d1_databases]]\nbinding = "DB"\ndatabase_name = "toscreen-share"\ndatabase_id = "${database.uuid}"\n`;fs.writeFileSync(path.join(cwd,'wrangler.generated.toml'),generated,{mode:0o600});console.log('Prepared wrangler.generated.toml. Next: wrangler d1 migrations apply toscreen-share --remote --config wrangler.generated.toml && wrangler deploy --config wrangler.generated.toml');
