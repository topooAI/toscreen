# AI Agent 行为准则 (AI Agent Code of Conduct)

这是 ToScreen 项目的全局强制规则文件。任何接入此项目的 AI 辅助编程工具（如 Cursor, Antigravity 等）必须严格遵守以下规则：

## 1. 核心工作流 (Core Workflow)
- **强制上下文加载**：在开始处理任何新的 Feature 开发、Bug 修复或架构调整前，**必须静默阅读** `docs/architecture/` 目录下的相关文档。
- **禁止“盲人摸象”**：如果你不清楚某个模块的底层设计哲学（如坐标系归一化法则、渲染层隔离），禁止根据常规经验直接修改代码。必须先查阅文档或向用户确认。
- **经验沉淀**：每次解决深度 Bug 后，必须主动提示用户是否将该“踩坑经验”写入对应的 `docs/architecture/` 文档中。

## 2. 架构红线 (Architectural Red Lines)
- **UI 与渲染分离**：React 组件（UI层）绝对禁止直接处理复杂的数学计算或原生 PIXI 实例的暴力销毁。必须依赖 `EditorState` 和底层引擎提供的生命周期钩子。
- **坐标系法则**：`zoomTransform` 等底层运算一律使用**全局屏幕坐标系（0.0 ~ 1.0）**，绝对禁止与局部蒙版（mask）尺寸发生混淆和二次计算。
- **固定机位原则**：Timeline 上生成的任何 Zoom Region 都是一个**固定镜头**，必须与用户的“绿色裁切框”保持 100% 绝对一致，严禁私自添加平滑跟随（Lazy Follow）导致画面漂移。
